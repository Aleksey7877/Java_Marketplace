spring.application.name=user-service

server.port=${SERVER_PORT:8081}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:1011/marketplace_users}
spring.datasource.username=${DB_USER:marketplace}
spring.datasource.password=${DB_PASSWORD:marketplace}

spring.grpc.server.port=${GRPC_SERVER_PORT:9091}